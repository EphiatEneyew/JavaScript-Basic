// function--building blocks in Js. it is a set of statements that perform a task.

//performing task
function greet(name, lastName) {
    console.log('Hello ' + name + ' ' + lastName);
}

//Calculating a value
function square(number) {
    return number * number;
}

console.log(square(5));
greet('Ephrem', 'Eneyew');
