// we need array to work with a list of thing. it is a datastructur wiche represent list of items
let selectedColors = ['red', 'blue']; 
selectedColors[2] = 1; // the length and type of array is dynamic.
console.log(selectedColors);
console.log(selectedColors.length);